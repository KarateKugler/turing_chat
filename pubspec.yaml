name: turing_chat
description: "welcome to 2025 bitches"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  supabase_api:
    path: packages/supabase_api/

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.8.3
  flutter_bloc: ^9.0.0
  flutter_animate: ^4.5.2
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true